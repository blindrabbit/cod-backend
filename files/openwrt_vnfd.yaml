vnfd:vnfd-catalog:
  vnfd:
  - connection-point:
    - name: vnf-data
      port-security-enabled: false
      type: VPORT
    # - name: vnf-mgmt
      # port-security-enabled: false
      # type: VPORT
    description: Generated by OSM package generator
    id: openwrt_vnfd
    mgmt-interface:
      # cp: vnf-mgmt
      cp: vnf-data
    name: openwrt_vnfd
    short-name: openwrt_vnfd
    vdu:
    - count: 1
      description: openwrt_vnfd-VM
      id: openwrt_vnfd-VM
      # image: cirrOS-0.4.0-x86_64
      image: ubuntu-cloud-20.04-atualizado
      # image: ubuntu-18.04
      # cloud-init-file: cloud-config.txt
      supplemental-boot-data:
        boot-data-drive: true

      interface:
      # Specify the external interfaces
      # There can be multiple interfaces defined
      - name: eth0
        type: EXTERNAL
        virtual-interface:
          type: PARAVIRT
        external-connection-point-ref: vnf-data
      # - name: eth1
        # type: EXTERNAL
        # virtual-interface:
          # type: PARAVIRT
        # external-connection-point-ref: vnf-mgmt
      # interface:
      # - external-connection-point-ref: vnf-data
        # name: eth0
        # type: EXTERNAL
        # virtual-interface:
          # type: PARAVIRT
      name: openwrt_vnfd-VM
      vm-flavor:
        memory-mb: 1024
        storage-gb: 10
        vcpu-count: 1
    # vnf-configuration:
      # juju:
        # charm: simple
      # initial-config-primitive:
      # - seq: '1'
        # name: config
        # parameter:
        # -   name: ssh-hostname
            # value: <rw_mgmt_ip>
        # -   name: ssh-username
            # value: ubuntu
        # -   name: ssh-password
            # value: 123456
      # - seq: '2'
        # name: touch
        # parameter:
        # -   name: filename
            # value: '/home/cirros/first-touch'
      # config-primitive:
      # - name: touch
        # parameter:
        # - name: filename
          # data-type: STRING
          # default-value: '/home/cirros/touched'    
    vendor: OSM
    version: '1.0'
